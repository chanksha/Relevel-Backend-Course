console.log("Hello World");

//commands -->
            // git status --too check status
            // git add .--to add a File
            // git commit -m "initial coommit" -- to replace it with your Message 
            // git push --set-upstream origin main -- for first time to upload File
            // then git push -- to upload files
